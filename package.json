{
  "name": "freedom",
  "version": "1.0.1",                       // ← minor bump after big update
  "description": "freedom WhatsApp Bot with Multi-Number Support",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^7.0.0-rc.9",   // ← Most active & updated fork in 2026
    "express": "^4.21.1",
    "fs-extra": "^11.2.0",
    "pino": "^10.1.1",                          // ← Major upgrade, very fast
    "jimp": "^1.6.0",
    "@octokit/rest": "^21.0.2",
    "child_process": "^1.0.2",                  // ← built-in, no need ^ version
    "denethdev-ytmp3": "1.0.3",                 // ← still same, consider replacement later
    "yt-search": "^2.12.1",
    "path": "^0.12.7",                          // ← built-in, usually not needed in deps
    "axios": "^1.13.2",
    "moment-timezone": "^0.5.46"                // ← 0.6.x was never really stable
  },
  "engines": {
    "node": ">=18.x"
  },
  "keywords": [
    "whatsapp",
    "bot",
    "multi-device",
    "baileys",
    "sula-md"
  ],
  "author": "freedom",
  "license": "MIT"
}
